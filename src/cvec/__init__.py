"""cvec - CLI tool for downloading, extracting, and searching CVE data."""

__version__ = "0.1.0"

# Manifest schema version - increment when parquet structure changes
# This must match the version in cvec-db
MANIFEST_SCHEMA_VERSION = 1
